# 提交与检查

- 开发、提交、推送和发布按用户要求分别完成。收到提交或推送请求时，按[测试说明](tests/README.md)运行或复用相关检查，再执行已授权的 Git 操作。
- `npm run check:plan` 说明本次检查范围；`npm run check:commit` 执行相关检查。功能修复还需覆盖原问题和受影响的真实页面；提交检查通过只代表所列范围。
- 推送完成以目标远端分支的提交号一致为准，远端检查状态单列。完整发布验收在正式发布阶段执行；普通提交不等待完整发布验收。
- 报告新增、修复、实际验证和未完成项，并分别说明提交、推送、发布状态。
