---
status: accepted
---

# 地标验收台完整预热当前地标的所有阶段

为方便连续对比，内部验收台准备当前地标全部阶段的三档图片，编号课通常 18 项，五状态支线通常 15 项；只预热当前地标，孩子端仍按需加载。

- 各视角共用图片，按美术版本缓存；切地点优先准备新地点，停止旧地点未开始任务。
- 新图未好时保留旧图；失败自动重试两次后显示具体资源，可单独重试或用兼容格式。
- 全部下载并可显示才标记就绪；之后切换不新增图片请求、不空白，100 毫秒内完成。
- 显示准备数量、体积和当前资源；复验只绕过当前地标缓存。接受每课约 2.5–3 MB 的审查流量。
