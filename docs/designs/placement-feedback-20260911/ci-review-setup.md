# 发布时发现的独立复习检查准备数据遗漏

2026-09-11，修复提交 `d61b8e7` 的 GitHub 流水线在 `npm run test:review-browser` 等待 `L49-STORY` 入口时失败。该脚本直接调用判分函数构造跳级通过记录，并把它当作已经完成结算。

新增末题反馈边界后，这份记录实际为 `status: passed`、`feedbackPending: true`、`skippedUntil: 0`。因此 Lesson 49 尚未解锁，等待课程入口会超时。这与已通过的官网真实操作不同：学习者点击末题“继续”后才解锁路线。

最小复现沿用原脚本的种子 81、目标 `lesson-49-50` 和二十道正确答案；“准备数据应解锁 Lesson 49”断言失败。修正后，准备数据通过正式运行时执行开始、听音、选择、检查和继续，包含末题继续，再保存供复习检查使用。额外断言结果页已进入且 `feedbackPending === false`；没有通过直接改写该标志伪造完成。

此前单元检查和完整页面矩阵没有复用这段准备数据，发布前也漏跑了这个独立复习脚本。现在已补跑完整独立复习检查：51 项场景通过，涵盖持久化草稿、离线刷新及进程重启、损坏恢复、旧输入记录迁移、Lesson 49–50 试点题和三道隔日复习题。存储浏览器检查也已通过。其他脚本与支持代码中没有发现同样的直接判分造档路径。

本次仅修正 `scripts/check-course-review.js` 及本记录。构建输入核对确认 3,022 个公开文件与 `d61b8e7` 逐字一致；原完整页面证明和音频证明仍适用于这些公开文件。重新发布时记录最终提交号，GitHub 新一轮流水线的结果单独记录，不把本地通过写成远端流水线已通过。
