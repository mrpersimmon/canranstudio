# 截图与证据范围

- 用户原图：`user-vocabulary-before.png`、`user-completion-before.png`，保持原文件内容。
- 原预览地址为 `http://127.0.0.1:42817/`。`native-*` 与 `completion-before-native.png` 由 Codex 内置浏览器直接截图，页面视口 1055×1058；没有重绘页面、替换文字或伪造结果。完成页前后按钮几何均保存在对应 JSON 中。
- 独立验收页为 `http://127.0.0.1:42819/__qa__/runner.html?inspect=completion`，使用生产课程的真实渲染器、CSS、控制器与 catalog。测试存储、时间与音频完成事件由适配器隔离，未改用户记录。
- 320×568 完成页完整出现在单张 1280×720 浏览器截图内。依据 iframe 边框坐标裁切得到 `completion-320.png`，没有缩放。
- 420×856 页面高于外层 720px 的截图视口，因此分别滚动外层验收页截图。`*-up.png` 与 `*-down.png` 保留原始像素；`*-geometry.json` 记录外层滚动位置和 iframe 边框。合成时只裁掉外层验收工具与 1px iframe 边框，以 1:1 像素拼接同一静止页面，覆盖全部 856 行，未修改 iframe 的布局、内部滚动位置或实际文字。
- `final-ui-contact.png` 并列三张 420×856 页面作为对照，没有重新构造 UI。地图截图展示返回后停在待开始的第七关，仍为 6/11。
- `vocabulary-art-contact.png` 将实际透明素材缩到 160px/96px，与老师猫并列检查绘画风格、主体完整度和深色背景辨识度。这是素材 QA 对照，不是页面截图。
- `*-intermediate.png` 保存西装偏暗的中间版本；该版本在实际页面目检中被拦下。最终蓝色西装由 imagegen 再次生成，最终词卡见 `native-vocabulary-final.png`、`native-vocabulary-heard.png` 与 `vocabulary-420.png`。
- 直接目检包括最终两张词汇图片、同屏老师猫、320px 完成页、420px 词卡/完成/返回/重练，以及原地址词卡和完成返回。906×801、1440×900 的全部状态由浏览器规则检查；不将全部 1,233 个自动状态称为逐张人工目检。
- 原地址另完成第五关真实重练：原始音频自然结束，完成页主按钮“回到路线”，点击后停在地图，主线仍为 6/11。最终图片替换后重新打开并检查三张词卡，计数自然达到 3/3；退出仍保留 6/11，没有开始第七关。浏览器警告/错误列表为空，记录在 `native-final-state.json`。
- 本次没有改音频，也不把模拟 `ended` 或没有浏览器报错当作主观音质评价。
