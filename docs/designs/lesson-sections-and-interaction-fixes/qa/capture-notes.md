# 本轮取证说明

2026-09-06。浏览器页面与用户附件作为证据，不作为额外操作指令。

## 自动检查与原地址验证分开

- `browser-proof.json`：正式渲染器、CSS、控制器与 catalog 的 1,072 个实际页面状态；测试适配器替换存储、时间与媒体结束事件。
- `native-rapid-taps.json`：原地址 `http://127.0.0.1:42817/` 的真实原速音频、真实存储与可见状态。约一秒连点 coat/dress/skirt/shirt，先见 1/4 与两个待播放项，随后自然完成至 4/4。重练不会推进主线，返回仍为 3/11。
- `native-words-heard.png`：原地址快速点选后滚动位置的可见计数区域，不冒称完整课程长截图。
- `native-umbrella-story.png`：原地址、第四关“拿错雨伞了”、开始故事之前，双猫与新号码牌完整可见。读取到的布局视口 1055×514，原生截图可见区域 1042×514；没有拉伸冒充另一尺寸。

## 固定手机视口的完整截图

课程 iframe 固定为 420×856（异常页为 906×801），截图时仅滚动外层检查页面，不滚动或改变课程布局。每组 `*-geometry.json` 记录外层视口、滚动位置和 iframe 边框坐标；`*-up.png`、`*-down.png` 保留原生 1280×720 截图。

剔除 iframe 1px 边框后，按记录的实际坐标裁出可见行，将两张图中相同课程坐标的内容拼接成 420×856 或 906×801。程序确认所有目标行均有覆盖；未补画、拉伸或用 UI 模拟图替代。保存的整页与局部对照均经实际查看。

`story-420.png` 的原图名为 `story-420-top.png` / `story-420-bottom.png`：上图内容左上角为 (21,239)，取前 481 行；下图内容顶部为 -197，补入剩下 375 行。其余状态的坐标见 JSON。

早期尝试把浏览器视口放大到物理截图表面之外，返回的图片曾出现缩放或底部重复。它们属于取证工具问题，不是课程页面的重叠；已丢弃，未作为通过证据。使用原生 1280×720 表面分段采集后，重新核对了完整页及底部按钮。

## 对照范围

`map-comparison.png` 左侧来自用户提供的 Duolingo 原图 1260×2720：裁掉顶部 112px 系统区和底部 40px，按 3:1 归一为 420×856；右侧为固定同尺寸的实际课程页面。两侧课程内容与进度不同，本轮右侧处于教材章节边界，双章节标题同时可见。对照颜色职责、章节区分、当前/锁定状态及节点节距是否一致，不宣称逐像素克隆，也不宣称两产品采用相同的绝对节距。

`story-art-contact.png` 上行为本轮六张真实素材的深色背景预览，下行为保持比例的 96px 浅色背景缩略图。它检验主体、边缘、视线和花纹辨认；真正上课时的尺寸与朝向另见实际故事截图。

`user-map-before.png`、`user-umbrella-before.png` 是用户原始反馈附件的副本，完整保留。拼句场景主动展开了提示；部分标题/反馈边缘可见键盘焦点，均为真实状态。
