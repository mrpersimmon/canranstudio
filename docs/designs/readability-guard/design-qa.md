# 可读性回归验收 · 2026-09-06

final result: passed

## 发现与原因

1. **P0：进入课程后几乎不可读。** 加载页直接声明 `body` 的深底白字，课内仅修改 `:root`；浅色气泡继承白字，角色图片与深色背景相乘。修复将加载样式限制在加载容器，课内根容器成对声明背景与文字，并隔离图片混合；气泡和弹窗明确拥有文字颜色。第一句对比度从 1.055:1 提升到 9.717:1。
2. **P1：进度变化后猫猫遮挡开始按钮。** 320×568、完成 5/11 关时，开始按钮中心命中的是 `journey-mascot`。猫猫改为与当前节点共用有预留高度的一行，站在另一侧，展开关卡时收起。自动回归包含全部进度的返回地图；另暂停在原失败状态复核截图。
3. **P1：词句题图片撑出场景，压住角色名。** 420 像素宽的“拼一句，问问手表”场景高 175px，但人物盒高 212.91px。网格行和人物最小高度补上约束，保持完整人物并防止越界。已新增图像边界检查，另暂停在相同活动目视复核。
4. **P2：两处小字对比度不足。** 地图章节说明原为 4.47:1，课内蓝色角色名原为 4.45:1；分别调深文字色，四种尺寸的实际对比度检测通过。
5. **P1：异常恢复可能遗留地图主题。** 控制器异常改用公共 `render` 路径，确保退出深色状态。保存失败弹窗也明确声明浅底深字。

## 视觉依据与比较方法

- 用户问题原图：`qa/user-report.png`，905×799；此图的完整浏览器视口未知，不用它推断底栏的布局差异。
- 既有浅色课内方向：`../lesson1-6-path-v4/qa/reference-story.png`。该图是开始故事前，作为主题和人物完整性的依据，不作为对话布局的像素比较。
- 修复前浏览器重现：`qa/before-story-906.png`；修复后：`qa/story-906.png`。两者均为 906×801 CSS px、906×801 原生 PNG、deviceScaleFactor 1，同为“Excuse me!”和“Yes?”两句。
- 全图并排：`qa/comparison-full.png`；重点区域并排：`qa/comparison-detail.png`。先按原生 PNG 中的 ICC 信息转换至 sRGB，再并排；保留原图，不缩放人物和文字。
- 手机真实入口：`qa/story-420.png`、`qa/handoff-420.jpg`、`qa/handoff-320.jpg`、`qa/reference-320.jpg`。JPEG 为浏览器截图接口实际返回格式，尺寸按 CSS 视口核对；精确对比度使用浏览器计算色值，不使用 JPEG 像素估算。
- 进度变化的地图截图：`qa/map-progress-5-320.png`，从 `qa/map-progress-5-source.jpg` 中裁出 320×568 的实际测试视口。裁切移除了检查工具界面，保留学习页面全部内容。
- 词句题修复：`qa/scene-fixed-420.png`，实际人物高度 159px，场景高度 175px，人物底边位于场景内 8px，角色名不再被压住。
- 异常恢复：`qa/blocked-906.png`，浅底、完整猫猫、错误说明和恢复按钮均可见。上述两图分别从 `qa/scene-fixed-source.jpg`、`qa/blocked-source.jpg` 中提取真实测试视口；左上角坐标均为 (21,239)，未缩放学习页面。

## 五项必查表面

| 表面 | 结果 |
| --- | --- |
| 字体 | 原有 Fredoka 英文、系统中文字体与字号保留；字体已加载。普通/当前气泡、角色名和操作文字可读。 |
| 间距与布局 | 主问题的同尺寸前后布局一致；修复图片越界和地图猫猫遮挡。320 窄屏历史对话可滚动，当前句与答题卡保持可见。 |
| 色彩 | 地图维持深色，课内、课本、完成与异常页面采用浅底深字。第一句对比度 9.717:1；自动扫描不接受未解决的对比度问题。 |
| 图像 | 沿用完整猫猫素材，未替换为占位图。人物、物品加载成功，教学图片不被压暗、不撑出场景；无需通过裁切人物补救。 |
| 文案 | 教材、题目、答案和儿童操作文案不变；未添加多余说明到学习页面。 |

## 检查方式与边界

首轮新防护网先在旧代码上失败，证据为 `qa/before-failure.json`，确实包含浅底白字、错误背景和三张变黑人物/物品图片。

使用内置浏览器运行共同检查页。四种视口各检查 242 个状态，覆盖每一个实际活动 ID；共 968 个状态、52 个活动、11 个节点。包含选择、错误、提示、正确、逐词播放完成、配对、复习、课本、完成、保存失败与控制器错误恢复。故意制造的浅底白字、深底混合和缺失图片全部被检出。

真实首页另检查了加载、已有进度恢复、两句故事、真实音频播放时继续按钮禁用、音频结束后手动继续、语境题选择和正确反馈、两只猫的交接选择、退出地图、展开课本。浏览器 warning/error 检查为空，记录于 `qa/real-entry-console.json`。测试适配器模拟音频完成和存储故障，本次不声称重新评价了音质。

发布包生成要求浏览器证据完整且指纹匹配。缺失记录已实际验证为非零退出；单元测试验证错误、缺少状态/尺寸/活动、无效负向对照和过期指纹均被拒绝。测试路由和适配器不进入 224 文件的发布包。

GitHub CI 已接入相同测试页的 Playwright 驱动；本次本地使用内置浏览器运行，未触发远端 CI 或部署。

最终浏览器证明归档为 `qa/browser-proof.json`，完成时间 2026-09-06 16:47:14（Asia/Shanghai）。其指纹为 `29d1c3776077f490950f3d777c45c90ab2c660cf6022b49b01982bc9641da44b`，与当前发布输入及检查代码匹配；`npm run verify:visual` 已通过。程序检查为 57 项通过，其中包含两项防护网契约测试。

修复提交 `b9a61d7` 后，`npm run verify` 完整通过：222 个课程依赖的清单一致、57 项程序检查通过、浏览器证明有效，最终发布包包含 224 个文件、3,420,609 字节。包内不含测试页面或测试适配器。该结果是本地构建，未上传或发布线上。

已重新启动用户原本的本地预览 `http://127.0.0.1:42817/`，重新检查资源后恢复其未完成的第 1 关，再显示原来的两句对话，未推进到后续题目。最终原生截图为 `qa/local-preview-906.png`；三张教学图片均加载成功，正文为 rgb(44,64,53)，课内背景为 rgb(223,230,223)，浏览器 warning/error 为空。截图完成后恢复正常浏览器尺寸，并关闭本次临时测试页面。

## 比较历史

- 旧地图验收的 `passed` 漏掉课内页面，不再被视为整套体验通过。历史报告开头已加入更正说明。
- 本次先记录 P0 并保持 blocked，再修复颜色边界，用同尺寸的前后图确认。
- 扩展回归继续发现并修复小字对比度、地图人物遮挡和场景图片越界，保留对应状态的目视检查。
- 第二轮完整检查在三个曾经失败的场景暂停做目视复核，随后完成全部 968 个状态并生成最终证明。所有已发现的 P0/P1/P2 均已修复；本次视觉验收为 passed。

## 完成清单

- [x] 修复前可重复检出原问题。
- [x] 四种尺寸覆盖全部课程活动及异常状态。
- [x] 负向对照证明确实能拦截本类故障。
- [x] 真实入口与用户问题同状态并排比较。
- [x] 修复场景截图、最终浏览器证明与防护网有效性检查归档。
