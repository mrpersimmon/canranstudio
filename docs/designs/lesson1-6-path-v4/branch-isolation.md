# Duolingo 独立分支整理

日期：2026-09-06。分支：`codex/duolingo-version`。从已记录线上发布结果的 `94dcf6942a4b19c0da28cf365fbb9dcbe816f6c9` 创建独立工作树，原开发目录、其他分支与线上站点保持原状态。

## 当前范围

保留 Lesson 1–6 的 11 个连续节点、52 个活动标识、39 句原课文、完整猫猫、原速语音、交互与记录逻辑；保留所用图片原稿和提示词、字体与图标许可、Duolingo 参考材料、现行方案、V4 页面和发布证据。

移除旧主页、Lesson 49–54、音标站点、旧课程运行代码、未使用资源及无关设计文档。旧独立课程页面不随本分支保留。原始 Git 历史可追溯；本次没有重写历史。

工作树从 2,006 个文件精简至 356 个，移除 1,658 个原文件，新增 8 个当前版本维护文件。文件清单见 [branch-isolation-inventory.json](branch-isolation-inventory.json)。资源目录中的 Lesson 1–2、3–4、5–6 名称表示当前课程所用素材来源，不代表保留了另一套网站。

## 独立维护

`content/learning-course.json` 是唯一课程编写入口，`content/textbook-sources.json` 保留 182 条原教材来源作为核对基准。读取与校验接口只依赖这两个文件，已不依赖旧总 catalog。原内容中的兼容字段与 `tests/unit/fixtures/lesson1-2-v36-catalog.json` 专用于现行产品的旧记录导入，不是旧页面。

构建器从当前 catalog、HTML、样式和脚本收集依赖，保留已核对的媒体字节。构建与本地预览无需第三方 npm 依赖。统一的单首页发布器保留输入须与 HEAD 一致的检查，并新增“编写内容必须与生成课程包一致”的检查。

## 已验证

- `npm run build:course` 成功，199 项资源，共 3,256,201 字节；与分支来源的课程包、生成 catalog、HTML 及每项资源逐字节一致。
- 课程清单 SHA-256：`2957c4a44b743bb38f1ef47b4ee8402b8dc9154e875e5ee7242bf34d556cd1b6`，与整理前相同。
- 当前范围 51 项自动检查通过，包括 11 节点完整流程、旧进度导入、逐句结束门控、错误重试、重练与首次证据隔离、跨课复习、保存异常、包完整性、全部资源 HTTP 响应与音频 Range。检查数量减少是因为旧网站和旧运行代码及其测试已移除。
- 独立预览 `http://127.0.0.1:42817/` 已由真实浏览器检查：首次 0 / 11、全部节点呈现、进入找回手提包、完整双猫、首句播放期间继续不可用，真实播放完成后可手动推进。播放第二句中刷新，再进入时保留第一句已听记录，未听完的第二句需要重新触发。所读 warn/error 日志为空。
- 本轮浏览器范围是独立分支的启动、首段交互与恢复检查，未重新执行全部节点的人工通关或逐条音质验收；既有完整课程证据见 [design-qa.md](design-qa.md)。

- 提交 `d3b33eed236c45bbc09354cc62fc9ff56d8f4244` 后执行 `npm run verify`，课程重建、51 项检查与正式发布包生成全部通过。产物只有一个 HTML 首页，共 201 个发布文件，加上内部发布清单为 202 个文件。
- 首页课程包 SHA-256：`bade38fefbe5a70958076d44da7fd7f756a040b6c163d7359cb73ad6ae44a1c9`，与原先已发布的首页包一致。
- 356 个跟踪文件中只有一个 HTML 入口；现存 Markdown 文档的本地链接全部可解析。当前工作树已独立提交，没有推送或重新部署。

正式产物在 `dist/learning-path/`，具体构建入口见 [README](../../../README.md)。本条记录更新只涉及文档；提交记录更新后再次构建，以使发布清单标记最终 HEAD。
