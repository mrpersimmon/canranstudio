# Lesson 49 v1.12：精简答题反馈

用户已确认本方案。本次范围是 `codex/butcher-version` 的 Lesson 49。

## 现在怎样反馈

- 全页移除“看看原因”入口。
- 答对：保留成功提示、音效和继续操作，不重复解释。
- 答错：语法、课文理解和拼句题直接显示原有纠错依据；分拣题继续按误选类别给出纠错。
- 听音题：保留重听、重选，不再显示单词释义式的解释。“老板的挑战”里的听辨题使用同样规则。
- 灯泡继续用于作答前的线索。自动出现的纠错不算主动使用灯泡，首次答错仍如实记录。
- 反馈区保留稳定高度，听辨反馈区收紧到一行提示所需空间；检查、重试时不挤动按钮。
- 锦囊纠错只保留本题所需的表达用途，移除导致小屏滚动的额外解释。挑战题删掉“这次把人物和食物都换了”这句无助于纠错的文字。

本规则取代 [v1.11 实施记录](2026-09-19-0216-v1.11-老板挑战与统一作答体验-实施记录.md)中保留折叠解释的规则。历史记录不改写。

## 实现与防护

普通练习和综合挑战共用反馈规则；分拣使用相同的成功反馈规则，保留已有的逐选项纠错。解释文字和正误状态一起进入读屏提示区。重试、换题、刷新按当前作答状态显示，避免上一题解释残留。

清理已移除入口的样式，并升级页面资源版本，避免刷新继续使用缓存中的旧反馈组件。

## 页面验证

沿用用户指定的页面行为测试。先复现两条未满足的新规则，再改实现：错题说明仍折叠；分拣答对后仍重复解释。

最终共 99 个页面行为用例通过，其中 10 个是本次新增的反馈专项。覆盖：

- 答错后直接说明；答对、重试、换题时清理说明；刷新恢复当前反馈。
- 听音寻宝与挑战听辨使用真实录音，播放结束后才允许检查；错答后可重听重选。
- 分拣四类别的逐项纠错；词块重新拼装；如实保留首次错误与灯泡使用记录。
- 320、390、768、1280 像素的纠错布局，以及其他回归中的 600 像素拼句和短屏操作。
- 各活动的结束、重练、下一站、统一灯泡、反馈音与缓存升级。

组合回归首次有 60 项通过、1 项仍采用旧的“错误标题后没有正文”预期。该预期按本次规则更新后单独复验通过；再完成 38 项音效、故事和拼句回归。以上不重复计数。另有首轮 45 项检查通过，与最终范围有重叠，不累加。

实际浏览器独立检查了错答、重试和答对画面。已刷新用户原预览，保留所在的证书页面、15 颗星及已输入的姓名。本次未改题目答案、进度存储格式和评分规则。

证据：[320 像素纠错画面](evidence/2026-09-19-2033-v1.12-精简答题反馈/纠错反馈-320.png)、[1280 像素纠错画面](evidence/2026-09-19-2033-v1.12-精简答题反馈/纠错反馈-1280.png)。同目录保存回归日志。

防护补充：旧检查关注反馈文字和按钮位置，未要求锦囊长说明完整容纳在小屏反馈区。本次新增 320 像素的全文可见检查，精简内容后通过。

## 交付状态

本地实施与上述页面验证完成。未提交、未推送、未发布；线上发布与真机验收不属于本次范围。
