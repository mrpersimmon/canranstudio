# 班级课程访问设计与验收 v1.1

日期：2026-09-25 11:51。状态：`codex/login-version` 本地实现；本版用学号和密码替代学习卡。未提交、未推送、未发布。账号升级与实际检查见[本次实施记录](../2026-09-25-1151-v1.1-学号密码实施与验收.md)。旧学习卡实现记录作为历史保留。

本文件承载班级访问的完整方案。[访谈记录](../2026-09-25-0021-v0.1-班级课程开放设计讨论.md)保留选择过程，[ADR-0022](../../adr/2026-09-25-0037-v1.0-ADR0022-班级课程访问控制.md)保留访问边界决定。课程内容继续依据[统一标准 v1.20](2026-09-25-1136-v1.21-教学单元设计标准与验收清单.md)，资源准备继续依据[缓存设计 v1.0](2026-09-24-2031-v1.0-网页加载缓存设计.md)；本方案的联网进入规则只在完成实施和迁移后替代原离线进入规则。

## 1. 已确认的范围

| 项目 | 已确认决定 |
| --- | --- |
| 课程范围 | A 班可只开放 Lesson 1–2；B 班可开放 Lesson 1–10，即五个教学单元。未开放课程不显示，直接课程链接也不能越权进入。 |
| 调整方式 | 管理员选择班级、勾选教学单元并保存，日常开课不重新发布代码。 |
| 学生身份 | 每人一个永久学号；使用学号和密码登录，初次登录或老师重置后必须先改密；班级由管理员分配。 |
| 班级归属 | 一名学生只有一个当前班级，可转班；学习成果仍归学生本人。 |
| 管理人员 | 首版只有用户本人一个管理员，不做多教师权限。 |
| 多端使用 | 允许多个登录端并存，首版不限制固定数量；微信与系统浏览器分别保存登录状态。 |
| 学习记录 | 同步已完成活动、星星与证书等成果；未提交题目的操作留在本机。旧匿名记录保留为本机旧记录，新身份从零开始。 |
| 访问资格 | 每次新进入课程先联网核验，图片等资源继续复用缓存；进入后短时断网可继续本轮。 |
| 公开入口 | 所有正式课程要求登录，没有公开体验课。 |
| 进度与权限 | 不要求完成上一课才能进入下一课；由班级开放清单决定能进入哪些课。 |

### 下架的七个独立课程

| 课程 | 原课程路径 |
| --- | --- |
| Lesson 49 肉店大冒险 | `/lesson/lesson49/` |
| Lesson 50 挑食小王子大冒险 | `/lesson/lesson50/` |
| Lesson 51 希腊四季之旅 | `/lesson/lesson51/` |
| Lesson 52 环球护照之旅 Ⅰ | `/lesson/lesson52/` |
| Lesson 53 英伦气候小主播 | `/lesson/lesson53/` |
| Lesson 54 环球护照之旅 Ⅱ | `/lesson/lesson54/` |
| 音标魔法乐园 | `/lesson/soundmark/` |

保留 Lesson 1–30 的十五个配对教学单元及 Lesson 49–50 教学单元，共十六个。管理员只从可用教学单元中勾选，不再出现“更多冒险”“单课练习”及音标乐园。

下架涵盖首页、课程登记、构建清单、直接地址及已知旧入口，不只删除卡片。旧课程地址显示“这节课已下架”和“返回课程”，不跳进其他课程代替。源码与历史记录保留；49–50 单元仍使用的图标和音频不能随单课一起删除。旧的固定资源副本与已经下载的页面无法被远程收回，下架承诺针对正式网站继续提供的访问。

## 2. 学生怎样使用

1. 第一次打开网站时，填写学号和初始密码。初始密码为老师核对过的姓名全拼，例如段晓东为 `duanxiaodong`；验证成功后只能进入改密页，设置新密码后才能进入课程。
2. 核验成功后，首页显示自己的名字、班级和该班开放的课程。没有开课时显示“老师还没开放课程”，不给出无效卡片。
3. 点击课程，先核对当前资格，再准备完整画面。缓存齐全时只核验资格和必要元数据，不重新下载整课。
4. 进入后，词卡、作答、重试、灯泡、进度和证书继续沿用统一标准，不把登录步骤插进每道题。
5. 完成一个活动后保存个人成果。换设备登录同一学号，可看到已同步的成果；正在选但未提交的答案不跨设备带过去。
6. 顶部可查看当前身份并“切换学生”或“退出登录”。切换前保存待同步记录，新学生看不到上一位的星星、草稿与证书。

学习设置使用课程共用的居中弹窗和浅底深字按钮，不能依赖浏览器默认居中，也不能单独使用白字按钮而遗漏背景色。关闭放在标题旁，切换学生和退出登录并排；重开本课放在分隔线下方，明确只清空本课成果，仍需再次确认。手机窄屏允许标题合理换行，按钮至少 44 像素；弹窗过高时在内部滚动。关闭或按 Escape 回到原活动和原焦点，不清空未提交答案。

旧链接的目标限定为站内合法课程：未登录先登录，核验通过再回到目标活动；无资格显示“这节课还没向你的班级开放”和“返回课程”。下架地址直接显示下架页。

学号是公开的账号标识，不是秘密；密码用于验证身份，不承诺阻止主动转借。丢失纸质账号单不丢失账号：老师按姓名或学号查找。忘记密码由老师核对学生后重置；不能仅输入姓名就自助重置。

## 3. 管理员怎样使用

管理页与学生页面分开，保留网站标志和按钮风格，主要提供三个区域：

- **班级**：创建、改名；进入班级后勾选开放单元并保存；可以暂时不开放任何课程。
- **学生**：输入姓名或课堂称呼、指定班级；支持逐个添加和多行粘贴；查看、改名、转班、停用与恢复。允许同名学生，但用独立编号区分。
- **学生账号**：可按学生或班级打印姓名、班级、学号和网站地址；仅在尚未改密时展示初始密码。已设置的密码不展示、不导出，老师也不能查看。后台支持全班级按姓名或学号查找，重名时以学号、班级辨认。
- **密码重置**：选定学生，核对姓名拼音，确认重置。旧密码和旧登录会话失效，下次登录必须改密。班级、学号和已完成成果保持不变；不是删除后重建账号。

普通转班不换学生身份、不清空成果。停用使用状态代替删除记录；首版不提供批量删除学生数据的快捷操作。管理员可以预览各班实际看到的课程，但预览不写入学生成绩。

管理员账号单独登录，不与学生账号混用。首次设置和账号恢复由站点维护者在服务器侧完成，不新增短信、邮箱或微信授权依赖。系统只需要课堂称呼和班级，不要求学生身份证、手机号或地址。

### 学号和姓名拼音规则

- 使用**已核对姓名拼音的第一个字母＋八位递增数字**；数字在全站统一计数，不按姓氏或班级重新计数。空库首位段晓东为 `d00000001`，随后李明为 `l00000002`，再录入段晓东为 `d00000003`。这对应本轮推荐方案；用户如果补充不同前缀要求，另行调整，不能暗中改已发出的号码。
- 添加成功时由数据库事务分配；同名允许、号码唯一。改名、纠正拼音、转班、停用、恢复或改密码都不重新分配。序号与数据库一同备份恢复，不由页面人数推算，不复用号码。
- 拼音在服务器本地转换，不向外部服务发送姓名。先用姓氏读法生成建议，老师确认多音字或生僻字；没有可靠结果时必须手填后才能添加。小写、不带声调和空格，`ü` 用 `v`。依据：[pinyin-pro 姓氏与无声调参数](https://pinyin-pro.cn/use/pinyin.html)。
- 拼音只用作初始密码，不因查看账号或修改称呼而重新生效。新密码为 8–64 位，含字母和数字，不得等于姓名拼音、学号或当前密码；两次输入必须一致。常用设备改密后保持登录，无需每次重新输入。
- 首次密码验证只建立 15 分钟的“待改密”会话，不能读取课程清单、课件、受保护资源或提交成果。改密成功更换会话，撤销该学生其他旧会话；普通学习身份仍记住 30 天。
- 后台重置也采用“待改密”流程；学生首页可以主动修改密码，主动改密需要当前密码。普通登录错误不区分账号不存在、密码错误或停用，避免提供账号探测信息。
- 姓名拼音与递增学号容易被猜到，强制改密不能消除首次激活前被猜中的风险；由老师把账号交给本人/家长并尽快完成首次改密。若以后需要抵御初始账号抢占，应改用随机一次性初始密码或另加老师确认步骤。本版按用户要求保留拼音规则，不宣称它是高强度初始密码。

## 4. 缓存与一次学习的边界

下列默认值已随整体方案确认。2 小时和 30 分钟均触发静默重新核验，不是限制学习时长或要求学生重新输入密码。

| 场景 | 建议规则 |
| --- | --- |
| 记住学生身份 | 常用浏览器最多保留 30 天登录状态，过期重新输入学号和密码。 |
| 新进入 | 首页点课、直接链接、新标签及页面刷新均先联网核验；失败时保留进度并显示重试，不先闪现课件。 |
| 同课内部操作 | 换题、换活动、阅读历史对白不算重新进入，不逐题等待联网。 |
| 一次学习 | 核验后 2 小时自动重新核验；有效资格且联网时保持题目和操作不变。短时断网可使用当前内容；核验失败保留操作，等待重试。 |
| 长时间放到后台 | 后台连续超过 30 分钟，返回时先重新核验；短暂切换应用不打断。 |
| 修改开放或转班 | 下次进入或重新核验按新范围执行，已经有效进入的本轮不突然清空。 |
| 退出、换学生、重置密码 | 旧身份不得继续新开课程；改密与重置使旧密码及旧登录失效。已经进入的本轮按上述有效期结束，不冒用新身份提交。 |
| 完全离线打开课程 | 不新开课，显示“联网后再进入”，允许重试和返回；资源和学习记录继续保留。 |

进入顺序固定为：确认学生身份 → 核验当前班级与课程 → 准备并核对完整资源 → 显示课件。保存资源与保存资格是两件事，命中资源缓存不能代替核验。

公共字体、标志和共用图片可以按文件内容复用；登录响应、个人课程清单和个人成果不得作为跨学生的公共缓存。活动草稿、待同步成果和个人显示状态按学生隔离。新学生无权进入上一位已缓存的课程；退出不删除其他学生的成果。

## 5. 学习成果怎样同步

每个成果包含学生、课程、活动及内容版本，保留现有各课程的真实完成条件。题目没做、答错或仅看过提示不能被同步成完成，证书仍受现有活动要求约束。

- 完成活动后先保存到本机的该学生记录，再同步服务端；网络失败排队重试，页面如实显示待同步状态，不谎报已经同步。
- 两台设备重复上传同一完成事件只算一次；一台较旧设备不能把另一台已完成活动改回未完成，也不能叠加星星超过上限。
- 本机未提交草稿只归本机当前学生，返回课程按已有恢复规则处理。重练和主动重置使用明确操作，不能被旧上传覆盖；冲突以对应重置代次和活动版本处理。
- 断网期间完成的本轮记录回网后仍归最初的学生；转班、课程收回和停用不把既有成果删除或转给其他人。
- 新登录前的匿名记录单独保留为“本机旧记录”，可查看原有摘要，不自动认领、上传或计入个人成果；已下架课程的旧摘要不提供重新进入课件的入口。

同步的是课程已有学习成果，不是新增考试认证或教师成绩统计系统；暂不加入逐题分析报表、排名和考勤。

## 6. 实现边界

现有静态课件保留，增加独立的班级访问服务和持久化数据存储。服务负责管理员、班级、学生、开放清单、登录、课程进入核验和个人成果。数据放在发布包之外，换版本或回退不能覆盖学生记录；上线前验证备份与恢复。

服务端是资格判断的最终依据。浏览器传来的班级号、本机标志、隐藏卡片和 Service Worker 不能授予权限。每个受保护请求都需有效身份及相应课程进入资格，访问另一学生的记录也必须拒绝。依据：[OWASP 授权指南](https://cheatsheetseries.owasp.org/cheatsheets/Authorization_Cheat_Sheet.html)。

入口、课包清单、教学内容、原始资源地址和固定资源地址使用一致规则。资源资格从课程的资源依赖清单派生：授权 49–50 教学单元应允许其所需的单课目录音频，不应顺带允许进入 Lesson 49 单课。共享文件不能按目录名粗略封禁。

学生密码使用带随机盐的 scrypt 单向摘要保存；只保留用于老师核对的姓名拼音，不保存学生自己设置的明文密码。学生及管理员登录使用 HttpOnly 会话、登录限速、会话过期与管理写操作来源校验。初始改密会话独立于学习会话，直接调用接口也不能绕过。管理接口不能用学生登录调用。会话防护依据：[OWASP 会话管理指南](https://cheatsheetseries.owasp.org/cheatsheets/Session_Management_Cheat_Sheet.html)；本方案中的各有效期是产品选择。

旧学习卡数据库原位升级：按原创建顺序分配学号和拼音初始密码，保持内部学生标识、班级和成果不变，撤销旧学生会话。旧学习码登录与二维码自动登录不再可用。若历史姓名无法可靠转拼音，账号标为待设置，管理员核对拼音并重置后再发放。升级前备份；重复启动不会重复分配、重置新密码或清空成果。兼容旧库的 `card-key` 仍与数据库一起备份，只用于读取/核对历史加密列，不再授予登录。

功能仅服务 `/lesson/`，不改变主官网。关闭脚本或入口脚本未加载时，禁用表单提交，不能将密码作为查询参数写进地址；只提供入口提示或下架页，不能回退到未经核验的完整课程。现有公开 V1 对身份接口的禁止需要按新边界正式修订，并新增相应验收；不能直接关闭检查。

### 已核实的改造重点

- 全部十六个教学单元都从 `requirePublishedCourse('lesson49')` 读取共用反馈定义，49–50 还读取教学数据。必须先将共享教学定义与公开路由状态分开，不能把 Lesson 49 从发布表一删了之。依据：[组合单元定义](../../../unit49-50/content.js)、[1–2 单元定义](../../../unit1-2/content.js)及其余各 `unit*/content.js`。
- [现有构建](../../../scripts/build-static.js)要求所有 `lesson*/index.html` 注册并复制相关目录；[课包生成](../../../scripts/course-packages.js)会扫描单元；[测试服务器](../../../tests/support/static-server.js)对未生成的路径会回落读取源码。下架需要同时明确公开入口与内部源码的边界。
- [资源发布](../../../scripts/stage-course-resources.js)保留历史文件，[Nginx 配置](../../../deploy/nginx/canranstudio-lesson-location.conf)目前公开提供旧固定资源与清单。新授权边界不能只套在最新版本的 HTML 上。
- `withdrawn` 目前只认精确的 `课程@版本`；旧加载器及 Worker 都先读缓存再检查更新。[旧缓存测试](../../../tests/e2e/course-cache-updates.spec.js)允许已撤回版本先显示到下一次操作，这一预期必须与“进入前核验”的新规则一并修订。既有测试通过不代表新边界已经满足。

## 7. 旧公开版本怎样过渡

1. 先在本地和测试环境完成新目录、下架页、学生入口、管理流程与真实课程验收；管理员身份、班级和学生账号准备就绪后，才具备发布条件。
2. 新发布清单只登记十六个教学单元；七项下架课程保留明确的不可用状态，不被自动扫描重新加入。
3. 发布时处理旧 Service Worker、旧课包、旧直接地址及固定资源入口。旧版本清单不能继续充当公开后门；新客户端在显示旧缓存课程前完成联网核验。
4. 迁移只调整课程资源缓存和访问路径；匿名记录单独保留，个人成果数据不随清缓存或发布回退丢失。
5. 已经离线保存的旧公开课件无法被远程即时收回。迁移验收分别验证“收到更新的在线浏览器”与“完全离线、未收到更新的旧浏览器”，不把后者说成可即时撤销。
6. 开启班级访问后的故障回退仍保持访问控制，不能直接回退成任意人公开可访问的旧官网；权限服务不可用时保留记录并提示稍后重试。

## 8. 验收清单

下表是验收合同，实际已验证范围和待验证项见[实施记录](../2026-09-25-1151-v1.1-学号密码实施与验收.md)。真机与生产验收单列，不因本地检查通过而标成完成。

| 编号 | 实际场景 | 必须看到的结果 |
| --- | --- | --- |
| A01 | 管理员建 A／B 班并保存开放范围 | A 只有 1–2；B 有五个单元至 9–10，保存不要求发布代码。 |
| A02 | 两位学生分别用学号密码进入 | 名字、班级和课程正确，不显示另一个班的内容。 |
| A03 | A 学生打开 B 专有课程直链、清单及资源 | 服务端拒绝，页面可返回课程，无完整课件闪现。 |
| A04 | 未登录、退出后或脚本关闭时打开课件 | 没有未经核验的课件或旧回退入口。 |
| A05 | 同机先 B 后 A，包括已缓存课程、历史返回和旧标签 | 资源缓存不授予 A 越权资格，不串星星、草稿和证书。 |
| A06 | 管理员改范围、转班、停用或重置密码 | 按本轮与新进入边界生效，旧密码失效，历史成果保留。 |
| A07 | 多端同时登录 | 常用端互不挤出，学生不能查看或修改别人记录及管理员内容。 |
| A08 | 完成活动后换设备 | 已同步成果出现，未提交答案不会被代答或误记完成。 |
| A09 | 两端交错完成、重试上传、内容升级与重练重置 | 不重复计星、不倒退、不恢复已重置旧结果、不伪造新版完成。 |
| A10 | 断网完成后回网，期间转班或同机换人 | 待同步记录仍归原学生，不能被当前学生认领。 |
| A11 | 新身份首次登录已有旧记录的设备 | 个人进度从零开始，本机旧记录仍在，不自动上传合并。 |
| A12 | 首次开课、弱网、图片失败、缓存复访 | 完整画面后进入；失败有重试；复访不重下整课；资格失败不能降级放行。 |
| A13 | 全离线新进入、学习中短时断网、2 小时到期、后台超过 30 分钟 | 分别按第 4 节执行，操作先保存，不以无限加载或删除记录处理。 |
| A14 | 首页和管理员选课列表 | 没有截图七项、没有空的“更多冒险”“单课练习”容器，保留十六个教学单元。 |
| A15 | 七个旧课程地址、index.html、已有缓存与旧版本链接 | 收到新规则后显示下架页并能返回，构建及更新不能将其复活。 |
| A16 | 十六个单元逐一进入并作答，完整学习 49–50 教学单元 | 共用反馈定义、图标、原录音、进度和证书可用，未因单课下架破坏启动或误删依赖。 |
| A17 | 管理员添加同名学生、批量添加、转班、重置密码和预览 | 对象清楚、学号一人一个、预览不改学生成果。 |
| A18 | 管理登录与数据恢复 | 学生无管理权限，错误登录限速；备份可恢复，部署回退不复写数据或重新公开受限课程。 |
| A19 | 安卓微信、iPhone 微信、手机系统浏览器及桌面 | 输入学号密码、首次改密、记住身份、课件进入、切换学生和复访实际可用，真机结果单独记录。 |
| A20 | 十六个单元分别打开学习设置，含桌面、手机、窄屏与横屏 | 居中、不横向溢出，普通按钮文字对比度至少 4.5∶1、点击区域至少 44×44 像素；确认文字完整可见。 |
| A21 | 关闭设置、Escape、取消重开确认，再次打开 | 当前活动、星星、未提交选择不变；重开须再次明确确认，切换学生和退出登录仍返回登录页。 |

账号升级额外验收：

| 编号 | 实际场景 | 必须看到的结果 |
| --- | --- | --- |
| U01 | 连续录入同名、异姓、不同班学生 | 全站数字顺序递增，重名不冲突，确认后号码不再改变。 |
| U02 | 多音字、ü、生僻字、批量录入 | 先核对拼音，可修正；非法拼音不提交，不部分添加。 |
| U03 | 初始密码登录后刷新、直链或请求课包 | 均先改密，不能展示课件或成绩；新密码校验明确且不泄露已有密码。 |
| U04 | 改密完成、退出及重新登录 | 初始密码失效，新密码可登录；其他旧会话不再有效。 |
| U05 | 忘记账号、老师查找及重置 | 姓名/学号可找回；指定学生重置，不能按同名误改整组；班级和成果不丢。 |
| U06 | 修改姓名、转班、停用、恢复 | 学号与已有密码保持不变。 |
| U07 | 旧学习卡数据库升级、重启、备份恢复 | 原学生成果保留、旧卡和旧会话不能登录；已发学号和新密码不被重置。 |
| U08 | 手机首次登录、改密与账号打印 | 表单、错误与按钮完整可见，无横向溢出；打印不暴露学生自设密码。 |

仍未解决的安卓微信录音问题单列，不因登录或下架功能验收而记为修复。教师教学验收、儿童试用也不由访问功能测试替代。

## 9. 实施与发布顺序

完整方案确认后依次完成：统一课程目录与七项下架 → 服务端身份和班级开放 → 学生入口与管理员管理 → 缓存迁移和成果同步 → 页面及访问边界验收。每步维护同一份课程范围，避免多个清单分别生效。

本次设计不包含收费、作业批改、多教师管理、自助注册、短信认证、招生试听或新的配音改造。正式实施时同步统一标准与缓存设计的适用边界，保留旧公开版本的历史验收记录。

开发、检查、提交、推送、正式部署和真机验收分别报告。完整方案确认只启动获准的实施范围，不把文档放入 `publish` 当成已经发布，也不自动执行 Git 推送或生产切换。
