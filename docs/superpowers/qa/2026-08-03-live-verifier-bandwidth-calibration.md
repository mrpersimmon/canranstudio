# 线上核验超时的修复记录

历史检查 · 2026-08-03。

候选版本切换后，多张大图和字体超过 10 秒未下载完，发布流程恢复了上一版。用上一版本复查，同样有 45 张图片超时，说明问题来自核验器的带宽假设。

并发从 8 改为 2，单项超时从 10 秒改为 60 秒后，上一版 597 项资源全部通过，耗时 276 秒。大小上限仍为 8 MiB，文件校验值、路径、响应头和跳转要求没有放宽。

当前命令为 `npm run verify:live`，见[发布指南](../../../deploy/README.md)。更长的核验超时不代表用户侧图片加载问题已经解决。
